var fullname : Array <string> = ["Freddy", "Kasimir", "Lesia", "Carlos"];

fullname.forEach(function(value,index){

console.log(`your name is: ${value} \n the index i: ${index} \n and both are: ${value}, ${index}`);

});// save